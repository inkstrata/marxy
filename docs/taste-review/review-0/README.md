# Taste review #0 — typeface pair

Both ADR-0015 candidate pairs on `fixtures/corpus/01-long-technical.md`, at the type scale on a
68 `ch` measure. Each passage is captured in **dark and light** (dark first, ADR-0024); **decide on
dark; confirm light does not change the choice**.

Regenerate with `node scripts/specimen/render.mjs`; `pnpm gate:specimen` checks the committed set.
