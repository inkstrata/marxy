# Plan delta — 2026-09-21 (MARXY-46/47 merge-bar: stale board rows)

PR #163 (MARXY-46) and PR #164 (MARXY-47) are signed and green, but the cycle merge-bar still
holds them: `orchestration/review.mjs` and `scripts/check-story.mjs` read `docs/plan/jira-issues.csv`
from **main**, while `origin/main` still carries the pre–light-variant row for MARXY-46 (dark copy,
`packages/theme/default` only) and a too-narrow MARXY-47 `Paths` cell. Each implementor PR also
self-widened its own CSV row — the defect MARXY-152 fixed for MARXY-138 — so those hunks must leave
the product branches before merge.

This pass lands **MARXY-176**, a board-only ops story: widen MARXY-46 and MARXY-47 on `main` to
match `docs/plan/tasks/MARXY-46.md` and `docs/plan/tasks/MARXY-47.md` and the file sets on #163
and #164, without `docs/plan/jira-issues.csv` inside either product story's `Paths`. After #176
merges, rebase #163 and #164, drop their CSV hunks, and the merge-bar can clear.

## Escalation risk

None for this landing story. MARXY-46 and MARXY-47 remain in_review until #176 is on `main` and
the two product PRs are restacked.

## Orchestrator — act on this

1. Merge **MARXY-176** first (board-only).
2. Rebase **#163** and **#164** onto `main`; remove each PR's `docs/plan/jira-issues.csv` hunk.
3. Re-run `node orchestration/review.mjs MARXY-46` and `MARXY-47`; sign approvals if boundary +
   acceptance tables match.
4. Merge #163 then #164 (or merge-bar order).
