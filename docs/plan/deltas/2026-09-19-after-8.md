# Plan delta — 2026-09-19 (after MARXY-8: fleet work that is not 65/77/106/107)

> Cadence pass. Trigger: `planner-trigger.mjs` YES — 5 merges since lastPlan
> (36 → 41). `2026-09-19.md` already exists; this file is the after-8 pass.
> Compute: minimal. No product code.

## What landed since lastPlan

`merges` 36 → 41. Recent landings: MARXY-92 (#56), MARXY-101 (#57),
MARXY-108 (#63 as `9a01de3`), MARXY-8 (#58 as `2b8a773`). 108 unblocked
`fast`. 8 landed biome.

## Standing decisions this pass does not reopen

- Do not re-plan, split, rewrite, unstick, or re-sequence MARXY-36, 59, 65,
  77, 91, 106, or 107. Closed PRs #28, #21, #51, #52 stay closed.
- Do not write a story that requires dispatching 65, 77, 91, 106, or 107.
- MARXY-20 stays `human-gated` (Ian's #59/#60). MARXY-21 (#61) and
  MARXY-23 (#62) are Ian's. MARXY-75 #55 is Ian / CODEOWNERS. MARXY-98 #42
  stays parked until Phase 2.
- Do not add `cross-phase` to 86/87. 86/87 remain absent from the CSV
  (observed, not restored — restoring them would re-open the 36 split).
- MARXY-94 (ADR-0026, shell-api) still needs Ian. Not boarded.

## Why ready offered only 65/77/106/107

`selectReady` picks eligible stories in CSV order and then treats later
overlaps as `blockedByPaths`. 65 occupies `package.json` and `ci.yml`
(blocking 82 and 91). 106 occupies `cycle.mjs` / `approve.mjs` (blocking
79). 77 and 107 occupy their own files. None of those four may be
dispatched this pass, so the board looked empty.

## Tripwires checked (`docs/roadmap.md`)

| Tripwire | State |
| --- | --- |
| Cold start > 500 ms after Phase 2 | Still stale. Ian withdrew the product budget. MARXY-110 accepts ADR-0029 and rewrites the tripwire. ADR-0013 is not edited. |
| Weight-harness residual > 25 on real Linux | No data. |
| `justif/core` cannot set ragged text | Not fired. |
| Reviewer fails the palette task at review #2 | Not reached. |
| Authoring re-enters scope; WebKitGTK < 2.50; single Tauri engine; first reaction about a feature | Not fired. |

Taste review: 8 / 92 / 101 / 108 changed nothing a reader sees. No new
queue row. No scope cut — Phase 0 is dispatch-starved, not behind.

## Decision 1 — Narrow 79 and 82 so they no longer sit behind declined keys

**MARXY-79** keeps the prompt-and-docs half only. Paths are now
`orchestration/prompts`, `orchestration/prompt-handshake.test.mjs`,
`docs/sdlc.md`. Cycle / approve / auto-merge stay on 106 (not required
to start 79). Acceptance is a file-read test of the two prompts plus
one SDLC sentence.

**MARXY-82** drops `package.json`. The selftest is
`node scripts/gate-protection.mjs --selftest`. No pnpm script. Live mode
still does not run in CI. Settings stay Ian's (MARXY-6).

## Decision 2 — Two new Phase 0 rows on free paths

**MARXY-109** (was `MARXY-NEW-outline`) — `outlineFrom` over the existing
AST. Design: `docs/design/12-outline.md`. Paths:
`packages/core/src/outline`, `packages/core/src/index.ts`. Deps: 11
(done). Does not touch sanitize (75), `package.json` (77), or Ian's
theme / typeset / fonts work.

**MARXY-110** (was `MARXY-NEW-no-ceiling`) — accept ADR-0029 and stop
printing 500 ms as a CI failure in `AGENTS.md` and as a Phase-2 tripwire
in `docs/roadmap.md`. Paths: the ADR, its README row, `AGENTS.md`,
`docs/roadmap.md`, `scripts/lib/no-ceiling.test.mjs`. Deps: 69 (done).
Does not edit ADR-0013 or `fixtures/perf-budgets.json`.

## Story changes

### Edited

- **MARXY-79** — Paths, description, acceptance (prompt slice).
- **MARXY-82** — Paths drop `package.json`; criterion 4 is the node
  invocation, not a pnpm script.

### Added

- **MARXY-109** — outline extraction. Phase 0. Deps: 11.
- **MARXY-110** — no-ceiling docs. Phase 0. Deps: 69.

### Dependencies

No new edges onto 65 / 77 / 91 / 106 / 107. No drop.

## How we would know I was wrong

1. **`ready.mjs` still offers only 65/77/106/107.** Then 79 or 82 still
   overlaps a picked path — name it; do not put `package.json` or
   `cycle.mjs` back.
2. **109 is returned for touching `packages/core/package.json`.** Then
   the export can live as a side-effect import from `index.ts` already
   on disk; do not widen Paths onto 77.
3. **110 edits ADR-0013.** Automatic return.
4. **A cycle dispatches 65, 77, 91, 106, or 107 because this pass added
   a dep on them.** Then the dep list is wrong — revert that edge.
5. **A second `jira.mjs sync` creates another outline or no-ceiling
   row.** Then the placeholders were not rewritten — stop.

## Follow-up — MARXY-111 lands this plan on main

79's implementor is blocked: worktrees are cut from `origin/main`, whose CSV
still has 79's old Paths, so `check-story.mjs` rejects
`orchestration/prompt-handshake.test.mjs`. 79 must not edit the CSV.

**MARXY-111** (was `MARXY-NEW-after-8-plan`) commits the after-8 files.
79, 109 and 110 now wait on 111. Paths are the plan files only — no
`cycle.mjs`, `ci.yml`, or `package.json`. 107 still lists the CSV / deps /
jira-map, so `ready.mjs` may show 111 as `blockedByPaths`; dispatch 111
anyway (107 is declined), then resume 79.

Do not re-plan 36 / 59 / 65 / 77 / 91 / 106 / 107.
