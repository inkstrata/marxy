# Plan delta — 2026-09-25 late

> Periodic pass. `orchestration/planner-trigger.mjs` said **yes: 7 of the last 10 merges were ops**
> (MARXY-43 is the only product merge in that window). The morning and evening delta is still open
> as **MARXY-212** (#195). Its evening summary tells the orchestrator to rebase MARXY-43 and to
> dispatch MARXY-216. Both are wrong now. This pass ran in `../marxy-wt/MARXY-219`, cut from
> `origin/main` at `90b975c` (MARXY-43, #199) by `out-of-plan.mjs start`. The orchestrator checkout
> `/Users/ian/Dev/marxy` and its `orchestration/state.json` were read and never written. Landing key
> **MARXY-219**, with its own `no-dispatch` row. The one new row was drafted as a placeholder and resolved to **MARXY-220** with
> `jira.mjs sync --new` before the PR opened.
> MARXY-216 is dropped on #195 in the same pass, because that row does not exist on `main`.

## What changed since the evening addendum

| Key | PR | Lane | Now |
| --- | --- | --- | --- |
| MARXY-43 | #199 | phase 3 | **Merged.** Toggle task and align table pipes. The rebase ask is discharged. Do not split it |
| MARXY-202 | #196 | ops | **Merged.** A dirty or ahead worktree reserves its paths. This is why MARXY-78 holds Phase 3 |
| MARXY-217 | #203 | ops | **Merged.** A pull request that conflicts with main stays In Progress, and the cycle starts one resolver (three tries, `attempts` unchanged). This is the owner MARXY-216 was going to be |
| MARXY-194 | #202 | phase 2 | **In review, CI red** on `fast` and `ci`. Gates passed. Holds the Phase 2 tail and, by path, MARXY-44 and MARXY-48 |
| MARXY-197 | #198 | ops | **In review, CI green.** Needs a reviewer |
| MARXY-212 | #195 | ops | **In review, CI green.** The evening text is stale; the drop below is on that branch |

No `results/*.notes.md` this window. `docs/taste-review/2026-09-review-2/decisions.md` is still blank.

## The finding that matters: a parked worktree holds Phase 3

MARXY-78 is `blocked` (parked 2026-09-20, two attempts, one of them a headless login failure). Its
worktree is **92 commits behind** `origin/main`, ahead 0, dirty. MARXY-202 skips a claim only when
the key is `done`. `ready.mjs` therefore excludes MARXY-44, MARXY-48 and MARXY-16 with
`worktree holds` by MARXY-78.

MARXY-44 and MARXY-48 are also inside MARXY-194's paths (`app.ts`, `notices`, `src-tauri/src`) while
#202 is In Review, so clearing the MARXY-78 hold does not make them ready today. It is what makes
them ready once MARXY-194, then MARXY-195 and MARXY-196, leave those files. Un-parking MARXY-78
would put the fidelity story back on `fixtures/corpus` and `src-tauri`, which is the reason it was
parked ahead of MARXY-138. Leave it parked.

### What this pass does

1. **One new ops story, MARXY-220.** `liveClaims` also skips `blocked` and
   `escalate`. A key with no `state.json` record still reserves, which is the out-of-plan case
   (MARXY-218, MARXY-211). It **proceeds** when `ready.mjs` and the README are free. It does not
   proceed ahead of a product story that is actually ready. Today none is. MARXY-218's dirty
   worktree holds `orchestration/ready.mjs` and `orchestration/README.md`, so this story waits on
   that session. Leave MARXY-218 alone; it is proposing ADR-0034 (review occupancy does not gate
   dispatch) and this pass does not re-file that.
2. **MARXY-216 is dropped**, on #195, with the reason in the addendum there. MARXY-217 already
   starts one conflict resolver and does not park the story. Shipping 216 would put a second,
   contradictory owner on `cycle.mjs`. MARXY-213's dependency on 216 is removed in that same
   commit, so a dropped story cannot hold 213 forever. **MARXY-213 stays deferred** until a cycle
   where a product story is also startable. 183 and 186 are already Done in the cache; the
   mechanism is still absent, and the ops tripwire is still fired.
3. **MARXY-44's corpus file is `22-unclosed-script.md`.** `16-api-reference.md` already uses 16.
   20 and 21 are MARXY-78's uncommitted fixtures. The card and the CSV Paths agree.
4. **MARXY-196 no longer cites ADR-0033 for the Rust index walker.** ADR-0033 is typography and is
   accepted. The walker stays unwired; this story adds no ADR.
5. **MARXY-48's card no longer cites the 600-line budget** MARXY-191 removed. Split only if a
   reviewer returns it, along 48a/48b.

No other split. MARXY-43 merged on attempt 2; the DIRTY return did not fail the story. MARXY-15,
MARXY-22 and MARXY-115 stay parked or dropped. MARXY-49 stays behind the Phase 2 boundary (it is
not `cross-phase`). MARXY-45, MARXY-97 and MARXY-184 still wait on dependencies.

## Re-sequencing

| Story | Change | Why |
| --- | --- | --- |
| MARXY-216 | label `dropped` on #195 | MARXY-217 is the conflict owner |
| MARXY-213 | deps no longer include 216; still deferred | a dropped dep would block it forever |
| MARXY-220 | new, ops, deps `[]` | a parked worktree must not hold Phase 3 |
| MARXY-44 | Paths file `16-unclosed-script.md` → `22-unclosed-script.md` | 16 is taken; 20 and 21 belong to MARXY-78's WIP |
| MARXY-196 | description and card drop the ADR-0033 pointer | that number is the typography ADR |
| MARXY-48 | card only | the 600-line split instruction was a removed gate |
| MARXY-43 | no row change | merged; no split |
| MARXY-78 | stays `blocked` | the new story releases the hold without running it |

## Ops this window (tripwire: ops > half of the last 10 merges)

| Story | Ruling | Why |
| --- | --- | --- |
| MARXY-220 | **Proceeds when its paths are free**, never ahead of a ready product story | it is what lets MARXY-44 and MARXY-48 start after the Phase 2 tail. Today the ready queue is empty and MARXY-218 holds `ready.mjs` |
| MARXY-213 | **Deferred** | does not unblock MARXY-194. Dispatch beside a product story, not alone |
| MARXY-16 | **Deferred** until Phase 3 has two stories In Review | unchanged. Also inside MARXY-194's `src-tauri` paths |
| MARXY-78 | **Do not un-park** | the hold is the bug; running it would take the corpus and the shell again |
| MARXY-216 | **Dropped** | see above |
| MARXY-197 | already In Review | not a new dispatch |

Nothing else in the ops lane is `todo` on `main`.

## Product order once MARXY-194 is green

`ready.mjs` sequences by path. CSV order offers MARXY-195 before MARXY-196; they share `main.rs`
and `app.ts`, so one at a time. MARXY-44 and MARXY-48 are `cross-phase` and share `commands` with
each other and `app.ts` / `src-tauri` with the tail, so they follow 195 and 196, one at a time.
MARXY-49 waits for Phase 2 to close. Then MARXY-97 (depends on 44), MARXY-45 (depends on 97),
MARXY-184 (depends on 49). Palette re-review stays queued until MARXY-196.

## Tripwires checked (`docs/roadmap.md`)

| Tripwire | Result |
| --- | --- |
| Ops > half of the last 10 merges | **Fired (7 of 10).** Deferrals above. The window is still the fleet repairing dispatch, plus one product merge (MARXY-43) |
| Weight harness residual > 25 on Linux | Not evaluated. MARXY-22 stays `human-gated` (author, 2026-09-22) |
| Cold-start inflation | No recorded number changed |
| `justif/core` cannot set ragged text | Not fired |
| Reviewer fails the palette task at review #2 | Not evaluable until MARXY-196. `2026-09-review-2/decisions.md` is blank |
| A supported distro on WebKitGTK < 2.50 | Not evaluated |
| Authoring re-enters scope | No |
| A first-time user's first reaction is about a feature | No reviewer session this window |

## Scope pressure

**No cut.** The previous pass said to propose `docs/scope.md`'s cut order if MARXY-193 and MARXY-43
were not both In Review. Both have **merged**. The first three cuts on that list have already
shipped: the light variant (MARXY-46), align table pipes (MARXY-43), and user theme loading
(MARXY-47, MARXY-177). Phase 2 and Phase 3 are waiting on one red pull request, not on scope.

## Taste review

MARXY-43 merged with no queue row. This PR adds one. PR #199 has no screenshot pair; the question
is whether a toggled task and an aligned table still read as the same book. MARXY-193's row is
already on `main`. No other visual merge this window is missing a row.

## Escalation risk

| Story | Risk | Why |
| --- | --- | --- |
| **MARXY-194** | **High** | CI is red. `packages/core/src/position/watch-rust.test.ts` compiles `watch.rs` alone and fails because `mod runtime` is not in that temp dir (`rustc` E0583). That test is **outside MARXY-194's Paths**. Fix it in `../marxy-wt/MARXY-194`. If the fix edits the test, the story widens its own Paths in that PR. A second red after the fix is a return. Do not split it for this failure. Do not start MARXY-195 or MARXY-196 until #202 is green or returned |
| **MARXY-48** | Medium–high | Unchanged: the largest Phase 3 story. Rust `reveal_in_editor` and the `toml` crate are new ground. The card no longer pretends a 600-line gate will split it |
| **MARXY-44** | Medium | First story to persist per-file grants beside the wide sanitiser policy, on `app.ts` after MARXY-193 and MARXY-194. The corpus filename was a trap; it is now `22-unclosed-script.md` |
| **MARXY-196** | Medium | First time the running palette searches a real index. The stale ADR-0033 pointer is gone so the implementor does not edit the typography ADR or wire the Rust walker |
| **MARXY-78** | Medium | Two attempts recorded, one a login failure. Stays parked. The worktree is 92 behind; a later attempt rebases onto `origin/main`, it does not start from `046362b` |
| **MARXY-220** | Low | A status filter. The miss that matters is treating a missing record as parked, which would release MARXY-218 and MARXY-211. The card forbids it |
| MARXY-197 | Low | CI green and In Review. Not a return |

## Findings recorded, no new story

1. **MARXY-194's red check is one test.** `fast` failed in `@marxy/core` on
   `the shipped Rust watcher covers in-place, atomic rename, delete and move`
   (`watch-rust.test.ts:15`). `gates (macos-latest)` and `gates (ubuntu-latest)` passed.
2. **MARXY-218** (`../marxy-wt/MARXY-218`) is dirty, ahead 0, no pull request. Its uncommitted row
   is "review occupancy does not stop implementor dispatch" and a proposed ADR-0034. Another
   session owns it. Review WIP is 2 of 4, so that change is not what emptied the ready queue.
3. **MARXY-211** is the reader-artifacts research, dirty, behind `origin/main`. Leave it.
4. `/private/tmp/marxy-pr147` is still prunable. `../marxy-wt/MARXY-16` is still at `afd69df`.
   MARXY-16 is deferred either way.
5. The evening ten-line summary on `docs/plan/deltas/2026-09-25.md` is superseded by the summary
   below. Merge #195 for MARXY-213 and the MARXY-216 drop; do not follow its "rebase #199" line.

## Ten-line summary for the orchestrator

1. **MARXY-194 (#202) is the product gate.** CI is red on `watch-rust.test.ts` (`mod runtime` missing from the standalone compile). Fix in `../marxy-wt/MARXY-194`; widen Paths only if that test changes. Do not start 195, 196, 44 or 48 until it leaves those files.
2. **MARXY-216 is dropped** on #195 (MARXY-217 is the conflict owner). MARXY-213 no longer depends on it and stays deferred. Merge #195 after that commit is green; ignore its evening "rebase MARXY-43" line — #199 merged.
3. **Dispatch MARXY-220** when MARXY-218 no longer holds `ready.mjs`. Not ahead of a ready product story. It is what releases MARXY-78's hold on MARXY-44 and MARXY-48.
4. **Do not un-park MARXY-78** this window. Do not clean or delete its worktree.
5. **Do not dispatch MARXY-16.** Review #198 (MARXY-197, CI green) after the product PR.
6. After #202 is green: MARXY-195, then MARXY-196 (palette re-review stays queued until 196), then MARXY-44 and MARXY-48 one at a time. MARXY-44's fixture is `22-unclosed-script.md`.
7. Leave `../marxy-wt/MARXY-211` and `../marxy-wt/MARXY-218` alone. Do not file ADR-0034 here.
8. No scope cut. Light, align-table, and user themes have already shipped. MARXY-43 is not split.
9. After this merges, run `node orchestration/jira.mjs sync` — MARXY-44 and MARXY-196 rows changed. `lastPlan` records itself.
10. Taste: MARXY-43's queue row is in this PR. `2026-09-review-2/decisions.md` stays blank until MARXY-196.
