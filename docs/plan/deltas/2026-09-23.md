# Plan delta — 2026-09-23

> Periodic pass: `orchestration/planner-trigger.mjs` said **yes — 5 merges since last plan; 6 of the
> last 10 merges were ops**. Ran in `../marxy-wt/MARXY-201`, cut from `origin/main` at `afd69df`
> (MARXY-199, #187) by `out-of-plan.mjs start`. The orchestrator checkout `/Users/ian/Dev/marxy` was
> read and never written; `orchestration/state.json` was read, never written. Landing key
> **MARXY-201**, carrying its own `no-dispatch` row. The one new row was drafted as
> a `MARXY-NEW-` placeholder (slug `worktree-reserves-paths`) and resolved to **MARXY-202** in this
> branch with `jira.mjs sync --new` before the PR opened.

## What merged since the last delta

| Key | PR | Lane | Note |
| --- | --- | --- | --- |
| MARXY-183 | #180 | phase 2 | Second launches and macOS open events route to the running window |
| MARXY-188 | #181 | ops | Needs-human rulings landed; MARXY-94 un-gated |
| MARXY-191 | #185 | ops | One GitHub read per cycle; orchestration tested in CI; the 600-line branch budget is gone |
| MARXY-190 | #186 | ops | Out-of-plan work is one PR with its own row, adopted and landed by the cycle |
| MARXY-189 | #184 | ops | The broad-review delta: MARXY-193 to 197 filed |
| MARXY-94 | #182 | ops | `Shell` amended once for v1 (ADR-0026 accepted) |
| MARXY-42 | #183 | phase 3 | Copy section and copy code block, from Mod+C and the palette |
| MARXY-199 | #187 | phase 1 | Typography follows the reader-typography research (ADR-0033 accepted) |

Two product merges that a reader would notice (MARXY-42, MARXY-199), one shell contract, five ops.
The ops-majority tripwire fired for the second pass running; the cause is below, and it is not
"too many ops stories" — it is that the product pipeline is held shut.

## The finding that matters: one unpushed branch holds nine stories

`git worktree list` shows `../marxy-wt/MARXY-198` on `fix/MARXY-198-fix-the-correctness-and-performance-find`:
two commits by the author dated 2026-09-22 23:13 and 23:23, **38 files, +785/−121, no pull request**.
It is an `out-of-plan.mjs start` branch carrying its own `no-dispatch` row ("Fix the correctness and
performance findings of the 2026-09-22 review"), so it is a legitimate change on the supported path —
and it is invisible to the board: the row is only on its branch, `ready.mjs` reads busy paths from
`state.json` alone, and the cycle's only mention of it is `worktree keep … — no finished PR`.

What it does, from its diff:

- **Most of MARXY-193.** `AppHandle.open(path)` and `currentPath()`; `openReplacing` behind a
  `serially()` chain so opens and mode switches never interleave; `teardownDocument()` destroying the
  typesetter, the one `ResizeObserver` and the Source editor; `PaletteDeps.openDocument?` so the
  palette calls `handle.open` instead of writing `#doc`; `rerenderFromBuffer` after a Source edit. Of
  MARXY-193's six criteria it satisfies the substance of 1–3 without their tests and leaves 4 (error
  path through `innerHTML`), 5 (`renderPath` fallback and the registry entry), the `at` offset and
  `debugCounts` untouched.
- **The files every remaining Phase 2 and ready Phase 3 story needs.** `app.ts` (+141), `main.rs` (+72:
  raw-byte IPC, per-root watcher filtering), `palette/{view,search}.ts`, `shell/tauri.ts`,
  `source/buffer-commit.ts`, `render/post.ts`, `packages/core/src/{buffer,parse,render,position,operations}`,
  `packages/typeset/src/index.ts`, plus new corpus-free tests.

Once the cycle settles MARXY-42 and MARXY-94 as Done (their PRs merged; `state.json` still says
`in_progress` — see the summary), `ready.mjs` would have offered **MARXY-193 on `app.ts`**, the exact
file that branch rewrites. Two rewrites of one file is the failure `AGENTS.md` names as costing more
than the work it saves.

### What this pass did about it

1. **MARXY-198's row and `deps.json` entry are now on `main`, byte-identical to the branch's**, placed
   where the branch places them (after MARXY-189 in the CSV; after MARXY-196 and after MARXY-189 in
   `deps.json`), so its rebase sees an identical addition and no conflict. `no-dispatch` means nothing
   is sent after it; it is `todo` in every mirror until its PR is adopted.
2. **MARXY-193 depends on MARXY-198** and is re-scoped to the residual: delete the `renderPath` fallback
   and make `openDocument` required; `open(path, { at })` landing through the node map after the grid
   pass; error path to `textContent`; `debugCounts()`; the Playwright open-path test. The card lists
   what MARXY-198 already landed under "do not redo". Criterion 6 (`at`) is new; the others are the
   original six, tightened. MARXY-194/195/196 still depend on 193 and inherit the wait.
3. **MARXY-44 and MARXY-48 depend on MARXY-198** too: 44 hooks the render call in `app.ts`, 48 adds a
   command to `main.rs` and `shell/tauri.ts`. **MARXY-43 does not**: its overlap is thirty lines of
   `copy-section.ts` / `operations.test.ts`, and it is the one product story that can start today.
4. **MARXY-43, 44 and 48 are `cross-phase`.** The 2026-09-22 delta put 193–196 in Phase 2 knowing it
   would hold Phase 3 "after 42 merges". 42 has merged, and the Phase 2 tail is now behind an unpushed
   branch of unknown landing date. Holding 43/44/48 — all deps Done, disjoint from 193–196 except where
   `ready.mjs` already sequences them by path — buys nothing. The label is the escape hatch the
   orchestration README names for exactly this; `ready.mjs` still holds each of them by path while any
   overlapping story is In Progress.
5. **One new ops story, MARXY-202 (worktree reserves paths)**: a worktree whose branch names a key,
   with commits or uncommitted work and no finished PR, holds that key's paths as an In Progress story
   does, and `ready.mjs` names it. The row for a key with no row on `main` is read from the worktree's
   own CSV, which is what out-of-plan branches carry. This turns the hand-wired dependency above into
   a mechanism. It is **deferred** (below).

**Needs the author, not a story: push `fix/MARXY-198-…` and open its PR.** Everything in Phase 2
after this delta, and two of the three Phase 3 stories opened here, are behind that one action. The
cycle adopts it on open, the reviewer judges it by its own row, and `ready.mjs` holds the overlapping
stories by path from adoption to merge. If the author instead discards the branch, a planner removes
the three `MARXY-198` edges and restores MARXY-193's original card from `git log`.

## Re-sequencing

| Story | Change | Why |
| --- | --- | --- |
| MARXY-198 | row + deps entry added on `main` (phase 2, `no-dispatch`) | so a dependency on it can exist and its rebase is clean |
| MARXY-193 | deps `[]` → `[MARXY-198]`; card and CSV Description/Acceptance rewritten to the residual | MARXY-198 landed the mechanism; this finishes it |
| MARXY-44 | deps += MARXY-198; label `cross-phase` | `app.ts` overlap; Phase 3 unheld |
| MARXY-48 | deps += MARXY-198; label `cross-phase` | `main.rs` / `shell/tauri.ts` overlap; Phase 3 unheld |
| MARXY-43 | label `cross-phase` | deps Done, low overlap, the product story that can start now |
| MARXY-202 (new) | ops, deps `[]` | the mechanism behind this pass's hand-wiring |

No story split. No story dropped. MARXY-15, MARXY-22, MARXY-115 stay as parked/dropped. MARXY-49,
MARXY-97, MARXY-45 and MARXY-184 keep their phase-3 hold (their deps are not Done anyway).

### MARXY-78: not split, and the attempt count is wrong

`orchestration/results/MARXY-78.log` ends in `Authentication required. Please run 'agent login'` — one
of its two attempts was a headless `cursor-agent` login failure, the failure mode MARXY-156 was
written to stop counting. The worktree holds coherent WIP: `gate-fidelity.mjs` +237/−14 (the NFD,
lone-CR and trailing-whitespace mutants named in the card) and two new fixtures with goldens. The
2026-09-22 pass asked for one un-parked attempt; none has run. Same ruling, with one addition an
implementor must know: `fixtures/corpus/20-nfd-decomposed.md` and `21-lone-cr.md` are new corpus
pages, so `gate-aesthetics` will create baselines under `fixtures/baselines/<engine>/` and demand a
taste-queue row. **MARXY-78's Paths do not include `fixtures/baselines` or
`docs/taste-review/queue.md`; the story widens its own Paths in its own PR** (never a "land the
widening" story), or renders the two fixtures unreachable to the aesthetics gate and says why. Split
only if that attempt fails.

## Ops deferred this window (roadmap tripwire: ops > half of the last 10 merges)

| Story | Ruling | Why |
| --- | --- | --- |
| **MARXY-197** deferral gate | **Proceeds — but not alone.** Dispatch it in the same cycle as MARXY-43, product first. | Its paths (`scripts/`, docs, `reviewer.md`) touch nothing product; lanes are uncapped, so it costs no product lane; and it is the gate that stops the 34/35/38/87 pattern recurring precisely while 193–196 land. Dispatched alone it is one more ops merge; dispatched beside 43 the window tilts product. |
| **MARXY-16** ship v0.0.1 | **Deferred** until Phase 3 has two stories In Review. | Unblocks only Phase 4 rows (52, 185); its `apps/desktop/src-tauri` path is contested by MARXY-48, 49, 97 and 194; and its acceptance ends in a human mounting a DMG. It becomes `ready` the moment 42/94 settle — the orchestrator does not dispatch it this window. |
| **MARXY-202** worktree reserves paths (new) | **Deferred**: only in a cycle where no product story is startable, or alongside one. | Filed so the finding is not lost; the hand-wired edges above cover MARXY-198 today. |

Nothing else in the ops lane is `todo`.

## Tripwires checked (`docs/roadmap.md`)

| Tripwire | Result |
| --- | --- |
| Ops > half of the last 10 merges | **Fired (6 of 10).** Deferrals above. Root cause is the Phase 2/3 hold, addressed by the `cross-phase` labels and the MARXY-198 ask. |
| Weight harness residual > 25 on Linux | Not evaluated; MARXY-22 stays deferred without a Linux desktop |
| Cold-start inflation | No recorded number changed this window |
| `justif/core` cannot set ragged text | Not fired; MARXY-199 tuned the ragged breaker's hyphen accounting inside ADR-0007 |
| Reviewer fails the palette task at review #2 | `docs/taste-review/2026-09-review-2/decisions.md` is still unfilled; the palette it would judge has an empty index until MARXY-196. Re-review after 196, as the last delta said |
| A supported distro on WebKitGTK < 2.50 | Not evaluated |
| Authoring re-enters scope | No. MARXY-198's "leaving Source splices only what changed" is fidelity, not authoring |

No scope cut: Phase 3 is not slipping for lack of work, it is held; this delta un-holds it.

## Escalation risk

| Story | Risk | Why |
| --- | --- | --- |
| **MARXY-193** | Medium | Now a residual on top of MARXY-198's `app.ts`; an implementor who has not read that diff will re-implement `serially`/`teardownDocument` and conflict. The card names what not to redo. Criterion 6 (`at` through the node map after the grid pass) is new and timing-sensitive. |
| **MARXY-44** | Medium | First story to touch `app.ts` after MARXY-198 and the first to combine the wide sanitiser policy (MARXY-96) with a persisted per-file store; contract-adjacent to `packages/core` sanitise policy. |
| **MARXY-48** | Medium–high | The largest Phase 3 story. Its card still says "if the diff passes ~600 lines, split at 48a/48b"; MARXY-191 removed that budget, so the split is now the implementor's judgement, not a gate. Rust `reveal_in_editor` + `toml` crate is new ground. |
| **MARXY-197** | Medium | Unchanged from the last delta: a gate that starts red by design; the allow-list must only shrink. |
| **MARXY-202** | Low–medium | The hermetic-default criterion (fixture tests never touch git) is the one an implementor is most likely to get wrong. |

## Findings recorded, no new story

1. **`state.json` lags `main`:** MARXY-42 (#183) and MARXY-94 (#182) merged but the mirror says
   `in_progress`, which is why `ready.mjs` reports MARXY-43/44/48 as `blockedByDeps` and MARXY-16/193
   as `blockedByPaths`. The next real (non-`--dry-run`) cycle should settle both; if it does not,
   `node orchestration/state.mjs done MARXY-42 MARXY-94`.
2. The stale worktrees the 2026-09-22 broad review named (`marxy-planner-integrity`, `friction`,
   `train`) are gone from `git worktree list`. Its "ADR-0033 one index, in core" proposal went with
   them; the number is now ADR-0033 *Typography follows the research* (accepted). If the one-index
   decision is still wanted it is a fresh ADR number.
3. `/private/tmp/marxy-pr147` on `chore/MARXY-166-trigger-ci` at `0b007a9` is a detached scratch the
   cycle keeps as "no finished PR". Nothing here depends on it; the orchestrator may remove it.
4. MARXY-198 carries its own taste-queue row and CHANGELOG line on its branch; nothing is owed here.
   MARXY-199 has its queue row (`2026-09-reader-typography/`). MARXY-42 is clipboard, not visual.

## Ten-line summary for the orchestrator

1. **Ask the author to push and open the PR for `../marxy-wt/MARXY-198`** — it is the gate on MARXY-193/194/195/196/44/48. Add it to `needs-human.md`.
2. **Settle MARXY-42 and MARXY-94 as Done** in `state.json` (their PRs merged) — the next real cycle should; else `state.mjs done`.
3. **Dispatch MARXY-43 and MARXY-197 together** in the first cycle after this lands; product first. Do not dispatch MARXY-197 alone.
4. **Do not dispatch MARXY-16** this window, though `ready.mjs` will offer it once 42/94 settle.
5. **Do not dispatch MARXY-202** (worktree reserves paths) except in a cycle where no product story is startable, or alongside one.
6. MARXY-193, 44, 48 wait on MARXY-198 by dependency; 194/195/196 on 193. All become `ready` in the cycle after MARXY-198 merges, sequenced by path.
7. MARXY-78: un-park for one attempt on its existing WIP (one of its two attempts was a login failure); it must widen its own Paths for baselines and the queue row.
8. No split, no drop, no ADR, no scope cut. `check-cards` and `phases.test` green in this branch; `sync --new` ran, no placeholder remains.
9. After this merges, run `node orchestration/jira.mjs sync` — MARXY-193/43/44/48 rows changed.
10. Queue a palette re-review only after MARXY-196; review-2's `decisions.md` stays unfilled until then.
