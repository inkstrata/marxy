# Plan delta — 2026-09-19 (cadence: shared files do not occupy)

> Tenth planner pass, first dated 2026-09-19. Previous: `2026-09-18-relax-os-gates.md`.
> Trigger: `planner-trigger.mjs` YES — 8 merges since lastPlan (19 → 27) and escalate/blocked
> MARXY-36, MARXY-59. Compute: minimal. No product code.

## What landed since lastPlan

`merges` 19 → 27. `origin/main` is `ad01d4e`. Board is 27 done, including 73 and 102.
MARXY-104 landed as #48 and is already `done` in `state.json`; it is now in
`deps.json` phase 0 so it is not an unphased ghost. Do not recreate it.

| Key | What it closed |
| --- | --- |
| MARXY-104 (#48) | House PR body required at create time. |
| MARXY-73 (#36) | Hostile fixture vectors. Already queued for taste. |
| MARXY-64 (#17) | Prose-volume corpus. |
| MARXY-66 (#13) | Font text files readable. `docs/conventions.md` is free. |
| MARXY-58 (#25) | ADR-0006 amendment. |
| MARXY-102 (#46) | `in_review` occupies listed paths. |

## Standing decisions this pass does not reopen

From the 2026-09-18 low-compute / parse-on-gates / in-review-occupies / relax-os-gates /
paths-and-sequence deltas:

- MARXY-90 owns the apt restore. Do not land #32 as MARXY-74. Do not open a Jira row for 74.
- Do not re-dispatch 59, 85, or 90. 91 depends on 90. Do not dispatch 91 or 105 while
  62/65/90 own `ci.yml`.
- MARXY-36 stays blocked (dropped). Do not add `cross-phase` to 86/87.
- After 91 merges, rebase PR #15 and re-review that head — do not re-dispatch 59.
- CSV uses 86/87 only (not 88/89).
- Taste review #0 is Pair A. ADR-0015 accepted. MARXY-20 unblocked; phase 1 waits on phase 0.

## Tripwires checked (`docs/roadmap.md`)

| Tripwire | State |
| --- | --- |
| Cold start > 500 ms after Phase 2 | Fired early. Ian withdrew the product budget. Text is stale; MARXY-69 lands Amendment 2 of ADR-0022. Do not edit the tripwire in this pass (not in any newly-started story's Paths). |
| Weight-harness residual > 25 on real Linux | No data. |
| `justif/core` cannot set ragged text | Not fired. |
| Reviewer fails the palette task at review #2 | Not reached. 86/87 wait on phase 0. |
| Authoring re-enters scope; WebKitGTK < 2.50; single Tauri engine; first reaction about a feature | Not fired. |

Taste review: nothing visual merged without a queue row. 73 already has one. 102 and 104
are orchestration. No new ADR file. No scope cut — Phase 0 is review-bound, not behind.

## Decision 1 — 36 and 59 stay as the prior splits left them

The existing split is complete.

- **MARXY-36** stays `blocked` / `dropped`. Acceptance lives on 86/87. Do not re-dispatch.
- **MARXY-59** stays `escalate`. After 91 merges, rebase PR #15 and re-review that new head.
  Do not re-dispatch. Do not flip `required:false`.

## Decision 2 — CHANGELOG.md is an implicit extra and does not occupy

`ready.mjs` offered nothing. Four phase-0 stories had deps satisfied and were
`blockedByPaths`:

| Key | Real collision | Shared-file collision |
| --- | --- | --- |
| MARXY-8 | `package.json` on 62/65/85/90 | — |
| MARXY-82 | `package.json` on 62/65/85/90 | CHANGELOG.md on 85 |
| MARXY-80 | none | CHANGELOG.md on 85 |
| MARXY-100 | none | CHANGELOG.md on 85 |

`docs/sdlc.md` definition of done already grants `CHANGELOG.md`, the taste queue, and the
story's result file as extras outside listed Paths. MARXY-80 criterion 3 already excludes
the same set from disturbance. Listing `CHANGELOG.md` in Paths made one in_review story
serialise every other ready story.

**Path rule (apply now, do not widen):** `CHANGELOG.md`, `docs/taste-review/queue.md`,
`pnpm-lock.yaml`, and `orchestration/results/*` are never listed in Paths and never occupy.
A CHANGELOG line remains required. Do not drop `package.json` or `ci.yml` — those are real
file collisions.

Removed `CHANGELOG.md` from the Paths of todo phase-0 rows 69, 70, 78, 79, 80, 81, 82, 100.
Left it on in_review rows 85 and 90 (do not rewrite a reviewing boundary). After this,
**MARXY-80 and MARXY-100 are ready** and their remaining paths are disjoint, so both may
start. 8 and 82 stay path-blocked on `package.json` until 62/65/85/90 leave review.

No new occupancy story on `ready.mjs`: that file is 81's next edit, and 81 waits on 80.
A mechanical filter in `pathsOf` would delay the review-order stack for a rule this pass
already applied to the CSV.

## Decision 3 — MARXY-69 records a cold start; it does not enforce 500 ms

Ian's ruling stands: no product cold-start budget; measure; resist inflation; MARXY-16
must not claim a time. The taste sheet names "ADR-0022 Amendment 2 (MARXY-103)". There is
no MARXY-103 row and there will not be one. **MARXY-69 owns Amendment 2.**

Re-scoped: k ≥ 5 certified cold launches, recorded procedure, measurement-only reference
mode (501 ms still exits 0), runbook forbids a duration on the tag. Do not edit ADR-0013
in that PR (not in Paths). AGENTS.md's 500 ms table stays stale until a later docs row.

## Story changes

### Edited

- **MARXY-16** — acceptance: no cold-start duration on the tag, notes, or CHANGELOG.
- **MARXY-15** — pointer to 69 updated (measurement, not 500 ms enforcement).
- **MARXY-69** — summary, description, criteria 3/5/6/10, Paths drop CHANGELOG.
- **MARXY-70, 78, 79, 80, 81, 82, 100** — CHANGELOG dropped from Paths.
- **MARXY-104** — added to `deps.json` phase 0 (already done on the board).

### Dependencies

No new edges. 91 → 90, 105 → 91, 81 → 80 + 102, 59 → 91 unchanged.

No drop. No new row. 86/87 unchanged. 88/89 stay out of the CSV.

## How we would know I was wrong

1. **`ready.mjs` still offers nothing after this CSV lands.** Then 80 or 100 still overlaps
   an in_review path that is not CHANGELOG — name it and do not invent a third occupancy story.
2. **80 and 100 are dispatched onto the same files.** Then `pathsOf` is matching `docs/` as
   a prefix; that is a MARXY-9 bug, not a missing dep.
3. **69 is dispatched while #27 is open.** Then the 71 dep is missing.
4. **A second `jira.mjs sync` creates MARXY-103 or a third apt story.** Then stop.
