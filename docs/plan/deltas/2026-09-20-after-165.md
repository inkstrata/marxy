# Plan delta — 2026-09-20-after-165 (planner ran; no board edit)

> Periodic pass, triggered by `orchestration/planner-trigger.mjs`: "6 of the last 10 merges were
> ops." `orchestration/state.json` at trigger time: `merges: 118`, `lastPlan:
> 2026-09-20T19:53:09.274Z`, `mergesAtLastPlan: 116` (2 merges since the last plan). The
> orchestrator checkout (`/Users/ian/Dev/marxy`) was on `main` at `4f3c913`,
> `orchestration/board-check.mjs` clean, compute `default`. This pass ran entirely in a fresh
> worktree, `../marxy-plan-2026-09-20-planner`, cut from `origin/main` at `4f3c913` — the primary
> checkout was read for context only (never checked out, reset or committed by this pass) and
> `orchestration/state.json` (gitignored, local to the orchestrator) was read there directly rather
> than copied in, since this pass makes no CSV or `deps.json` edit for a dry-run to validate.

## The finding: the trigger is real, but the queue behind it is not an ops problem

The last 10 finished stories by `finished` timestamp are 6 ops, 4 phase:

```
MARXY-163  phase-1  PR #146   MARXY-165  ops      PR #144
MARXY-38   phase-2  PR #141   MARXY-161  ops      PR #139
MARXY-76   phase-1  PR #138   MARXY-162  ops      PR #140
MARXY-31   phase-1  PR #136   MARXY-126  ops      PR #134
                               MARXY-155  ops      PR #132
                               MARXY-156  ops      PR #127
```

Every one of those six ops merges is accounted for by work this planner's own prior passes asked
for, not by ops work generated for its own sake:

- **MARXY-165** is the previous plan delta landing (`2026-09-20-after-161.md`) — the mechanical
  carry every plan delta needs, since a story may not edit its own board row and the planner does
  not merge (`docs/plan.md`'s "how the loop runs").
- **MARXY-161** and **MARXY-162** are direct fixups of that same landing: a CSV dedupe PR #134
  missed, and an unused-import lint break the dedupe left behind.
- **MARXY-126** is the board-hygiene story (`check-cards.mjs`) the after-161 delta's own
  predecessor asked for.
- **MARXY-155** and **MARXY-156** are named, one-story-each fixes from the after-161 delta's own
  predecessor (`2026-09-20-after-154.md`): real before/after PNGs for MARXY-138's queue row, and
  the headless-`cursor-agent`-auth dispatcher guard.

None of these were ops stories competing with phase work for a dispatch slot; the phase work in
the same window (MARXY-163, MARXY-38, MARXY-76, MARXY-31) landed alongside them, not after them.
Checked against `docs/roadmap.md`'s tripwire text directly — "the planner defers ops stories that
do not unblock a phase, and says which ones in its delta" — there is no such story to defer today.
`node orchestration/ready.mjs` in the orchestrator checkout offers exactly two stories:

```
"ready": [
  {"key": "MARXY-164", "summary": "Wire highlighted code into the desktop render pipeline and theme"},
  {"key": "MARXY-118", "summary": "Remove a story worktree when its PR merges or closes, and report strays"}
]
"blockedByDeps": ["MARXY-16","MARXY-33","MARXY-36","MARXY-37","MARXY-39","MARXY-41".."MARXY-54","MARXY-86","MARXY-87","MARXY-96","MARXY-97"]
"excluded": [{"key":"MARXY-22","rule":"human-gated"},{"key":"MARXY-94","rule":"human-gated"},{"key":"MARXY-122","rule":"human-gated"}]
```

MARXY-164 is phase-1 product work (the real remaining phase-1 gate the after-161 delta already
identified). MARXY-118 is the one ops story in the ready set, and by the time this pass checked it
had **already merged** — PR #128, `mergedAt 2026-09-20T20:26:57Z`, is on `origin/main` at
`86888f7`, ahead of the `4f3c913` this pass and the trigger both read. `state.json` has no
`MARXY-118` entry (defaults to `todo`), which is the state.json-side drift the after-161 delta
already named for this exact key (its PR #128 was open then; it is merged now, and the local
mirror has not caught up either time) — not a new finding, and not something this pass, cut from
`origin/main` with `state.json` out of bounds, can write. Everything else in `blockedByDeps` is
genuinely gated on `earlierPhaseOpen` holding phase 2 until phase 1's real remaining gate
(MARXY-164) lands, exactly as `docs/plan/deltas/2026-09-20-after-161.md` described and left it.

## Why no board edit

There is no failed-twice story to split, no diff over ~600 lines, no undispatched ops story ahead
of phase work to defer, no tripwire in `docs/roadmap.md` firing for the first time (this is the
third consecutive pass to see "half the last 10 were ops" — after-154 at 8/10, after-161 at 6/10,
this one at 6/10 — and each time the composition has been the mechanical cost of landing the
previous delta, not compounding ops growth), no visual work that merged without a taste-review
queue row (MARXY-164's own Acceptance already requires one when it lands), and no decision that
changed under a contract. `docs/scope.md`'s cut order is not relevant — the phase is not behind;
phase 1 has one real story left. MARXY-15 and MARXY-78 stay parked exactly as the last two passes
left them; nothing here touches them.

## Two things named for the orchestrator, not fixed here

`orchestration/state.json` is out of bounds from this worktree (gitignored, local, edited only by
the orchestrator's own checkout), so these are read-only findings:

- **Reconcile `MARXY-118` to `done`** (PR #128 merged) before the next `ready.mjs` run offers it
  again for dispatch — the same class of drift the after-161 delta named for this key when its PR
  was still open, now on its second occurrence for the same story.
- **PRs #137 (MARXY-33), #142 (MARXY-37) and #143 (MARXY-36)** are all open and now
  `mergeStateStatus: DIRTY` / `CONFLICTING` against `main`, most plausibly because PR #145
  (MARXY-86, palette model) landed after they were opened and touches adjacent desktop surface.
  None of the three is currently offered by `ready.mjs` (`earlierPhaseOpen` still holds phase 2
  until MARXY-164 lands), so this is not an active dispatch hazard today, but whoever reconciles
  MARXY-33's long-standing `state.json`/PR-137 mismatch (also named in after-161) should rebase or
  close-and-redispatch these three before phase 2 opens for real, not after.

## What does not move

No CSV row. No `orchestration/deps.json` edit. No task card. No ADR proposed. No taste-review
queue row. MARXY-15 and MARXY-78 stay parked. MARXY-33, MARXY-36, MARXY-37, MARXY-118 are not
reconciled, merged, closed or reopened by this pass — that is `state.json` work, and `state.json`
is not this pass's file.

## Ten-line summary for the orchestrator

1. **No board edit was needed.** The ops-heavy trigger is real but fully explained by three
   consecutive passes' own necessary landing/fixup stories, not by runaway ops generation or an
   ops story blocking phase work; nothing in `docs/roadmap.md`'s tripwire response ("defer ops
   stories that do not unblock a phase") applies today because no such story is in the queue.
2. **Record this pass:** run `node orchestration/state.mjs planned` in the orchestrator's own
   checkout (not this worktree) to set `lastPlan`/`mergesAtLastPlan` and clear the trigger.
   `orchestration/state.json` is gitignored and local; this pass does not and cannot write it.
3. **Reconcile `MARXY-118` to `done`** in `state.json` — PR #128 already merged (`86888f7`,
   `mergedAt 2026-09-20T20:26:57Z`), before dispatching it again as if it were fresh.
4. **Dispatch MARXY-164** — the one real ready story, unblocked, no path conflict with anything
   in flight right now (PR #137/#142/#143 are all phase-2 and not offered by `ready.mjs` yet).
5. **Rebase or re-triage PR #137 (MARXY-33), #142 (MARXY-37), #143 (MARXY-36)** before phase 2
   opens — all three are `CONFLICTING` against `main` post-MARXY-86, and MARXY-33's `state.json`
   entry still does not record PR #137 (named already in `2026-09-20-after-161.md`).
6. **MARXY-15 and MARXY-78 stay parked** — no action, no change to either's `parkedReason`.
7. **Once MARXY-164 is Done**, phase 2 opens for real: MARXY-33, MARXY-36, MARXY-38 (already
   done), MARXY-86 (already done), MARXY-87 become dispatchable subject only to their own
   dependencies and path overlap, exactly as the after-161 delta projected.
8. **No ADR proposed.** No decision changed; no contract touched.
9. **No taste-review queue row added.** Nothing visible merged without one this pass; MARXY-164's
   own Acceptance already requires its own row and PNGs when it lands.
10. **`node orchestration/jira.mjs task` minted MARXY-166** for this pass's own landing PR (this
    delta plus its `CHANGELOG.md` line) — a Task, not a Story, and no CSV row, because this pass
    makes no board edit for `jira.mjs sync` to carry.
