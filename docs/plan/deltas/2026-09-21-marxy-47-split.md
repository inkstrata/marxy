# Plan delta — 2026-09-21 (MARXY-47: package vs shell)

> Trigger: planner pass `5692a368` after reviewer `10851ca3` returned PR #164 (MARXY-47 @ `79a0cea`).
> MARXY-176 (#166) is merged; main's MARXY-47 row still matches the pre-split task card (one story
> for loader + shell). Two blockers remain on #164: Acceptance names `gate:no-network over
> fixtures/themes/hostile/` on MARXY-47 while the task card assigns that gate to MARXY-45, and the
> diff is ~1235 lines against a ~600-line reviewer bar. This pass lands **MARXY-178** (`MARXY-NEW-land-marxy-47-split` before sync),
> a board-only ops story, in a worktree off `origin/main`, never the orchestrator checkout.

## The split

**MARXY-47 keeps its key and narrows to the theme package.** Loader, `rewriteUrls`, `setTopLevelKey`,
and the `quiet` / `contract-2` fixtures — everything provable with unit tests and no desktop shell.
Hostile `fixtures/themes/hostile/` and `gate:no-network` stay on **MARXY-45** (the task card already
said so; MARXY-176 only widened Paths on main to match the unsplit card).

**A new story, MARXY-177 (`MARXY-NEW-user-theme-shell` before sync), takes the app half** — the same vertical slice shape as
MARXY-37 → MARXY-169: `startUserTheme`, theme-as-document notice, harness acceptance, and a taste-queue
row because a reader sees user-applied theme for the first time. Depends on package MARXY-47 plus the
shell deps the old row carried (Source default, config, images scope).

## Dependency edits

| Story | Was | Now | Why |
| --- | --- | --- | --- |
| MARXY-47 | deps: 20, 37, 38, 95, 138; wide Paths + five acceptance checks | deps: 20, 138; package Paths; unit-test acceptance only | Package slice |
| MARXY-177 | — | deps: 47, 20, 37, 38, 95, 138; desktop Paths + harness acceptance | Shell slice |
| MARXY-45 | depends on MARXY-47 | depends on MARXY-177 | Gate loads hostile theme as **user** theme |

## Escalation risk

| Story | Risk |
| --- | --- |
| MARXY-177 | First reader-visible user theme + notice; taste queue required |
| MARXY-45 | Hostile fixture + `gate-no-network.mjs` over packaged renderer once shell lands |
| MARXY-47 (package) | Low after acceptance trim; second attempt if implementor botches the split |

## Orchestrator — act on this

1. Merge **MARXY-178** (board-only) before re-dispatching MARXY-47.
2. Split PR #164 into **PR A (MARXY-47 package)** then **PR B (shell key)**; drop CSV hunks from product branches.
3. Hold #164 merge until the board split is on `main`.
4. Re-run `node orchestration/jira.mjs sync` after merge if Jira was not synced pre-merge.
5. Point **MARXY-45** implementor at the shell story, not package-only MARXY-47.
