# Plan delta — 2026-09-25

> Periodic pass. `orchestration/planner-trigger.mjs` said **yes: 7 of the last 10 merges were ops**.
> The pass ran in `../marxy-wt/MARXY-212`, which `out-of-plan.mjs start` cut from `origin/main` at
> `d373abc` (MARXY-210, #193). The orchestrator checkout `/Users/ian/Dev/marxy` and its
> `orchestration/state.json` were read and never written. The landing key is **MARXY-212**, with its
> own `no-dispatch` row. The one new row was drafted as a `MARXY-NEW-` placeholder (slug
> `merged-settles`) and resolved to **MARXY-213** with `jira.mjs sync --new` before the PR opened.

## What merged since the last delta

| Key | PR | Lane | Note |
| --- | --- | --- | --- |
| MARXY-198 | #190 | phase 2 | The author's review fixes: one serial open path, `teardownDocument`, the palette opens through `handle.open`. It merged minutes before the 2026-09-23 delta landed, so the "push MARXY-198" ask in that delta is **discharged** |
| MARXY-201 | #189 | ops | The 2026-09-23 plan delta |
| MARXY-207 | #191 | ops | The fleet model matrix, pinned by a test |
| MARXY-208 | #192 | ops | Leased detached workers, a reaper, one cycle at a time, `doctor.mjs`, `loop.sh` |
| MARXY-210 | #193 | ops | `state.json` read-modify-write under one lock |

Open: #194 (MARXY-209, `pnpm done --open` pushing branches that track `origin/main`), ops.

**No product story ran between 2026-09-23 and 08:37 today**, when MARXY-193 was dispatched. The 2026-09-23 cycle launched MARXY-16, 43, 193, 197
and 202 inside a short-lived shell. The shell's exit killed all five before they wrote anything, and
the rows held their paths for 30 hours (MARXY-208's description). MARXY-208 and 210 are the repair,
and `reap.mjs` refunded the rows on 2026-09-25. The loop is **not running** now (`loop.sh status`).
So the ops majority is the fleet repairing its own dispatch, not ops work crowding out product.
Product is still where it was on 2026-09-23.

## The finding that matters: merged stories that the board reads as `todo`

`ready.mjs` treats a key with no `state.json` record as `todo` (`statusOf`, `ready.mjs:43`). Checking
every CSV row against `main`'s squash subjects finds three merged keys that the cache does not
record:

| Key | Merged | Labels | What `ready.mjs` does with it today |
| --- | --- | --- | --- |
| **MARXY-183** | #180, 2026-09-22 | `phase-2,desktop,out-of-plan`, **no `no-dispatch`** | offers it as **ready**, and its paths (`app.ts`, `main.rs`, `shell/tauri.ts`, `capabilities`, `tauri.conf.json`) put **MARXY-193, MARXY-44 and MARXY-48 in `blockedByPaths`** |
| **MARXY-186** | #171, 2026-09-21 | `ops,out-of-plan`, no `no-dispatch` | excluded only because it yields to MARXY-183, so it would be offered next |
| MARXY-210 | #193, 2026-09-25 08:30Z | `ops,out-of-plan,no-dispatch` | refused as `no-dispatch`; merged after the last full cycle, so `adopt.mjs` (which settles a merged `no-dispatch` row) should record it next cycle. It is in the command below only to be sure |

`jira-map.json` renames none of them, so the key-healing in `lib.loadState` did not drop them. They
are out-of-plan PRs that merged without the cycle adopting them, so no record was ever written.
MARXY-190 settles such a merge only for a row labelled `no-dispatch`, and MARXY-183 and MARXY-186
predate that label, so nothing will ever settle them.

The next cycle would therefore **dispatch a second implementation of MARXY-183**, merged work, on the
exact files the whole Phase 2 tail and two Phase 3 stories need. After that it would dispatch
MARXY-186. The real Phase 2 and Phase 3 queue is stuck behind a phantom.

### What this pass does about it

1. **The orchestrator runs, before the next cycle:**
   `node orchestration/state.mjs done MARXY-183 MARXY-186 MARXY-210`.
   This writes the local cache, not the board spec, as the 2026-09-23 delta asked for MARXY-42/94.
   The planner does not write `state.json`. **MARXY-193 was dispatched at 08:37 during this pass**
   (attempt 1). Its worktree was recut at `d373abc` and is editing `app.ts`/`palette/view.ts`, so it
   now holds `app.ts` itself. But the phantom comes back the moment 193 lands, unless the cache is
   fixed. A simulated `selectReady` with the three keys settled and 193 in progress gives: ready
   **MARXY-43**, 197, 202; **MARXY-48** held by 43 (`apps/desktop/src/commands`); **MARXY-44** held
   by 193 (`app.ts`) and 43 (`commands`); MARXY-16 yields to 48 and 194–196.
2. **One new ops story, MARXY-213**, so this cannot happen again. The cycle reads
   `origin/main`'s squash subjects once per cycle. It settles as done any key ending `(KEY) (#n)`
   whose record is missing or `todo`. It names any merged key the board thinks is live, without
   writing it. `ready.mjs` refuses a merged key with the rule `merged on main`. It is ops but it
   guards the product lane, so it goes **ahead of MARXY-202**, which shares `ready.mjs`, `cycle.mjs`
   and the README with it.

## Re-sequencing

| Story | Change | Why |
| --- | --- | --- |
| MARXY-193 | no row change; **in progress** (dispatched 08:37, attempt 1) | deps Done (MARXY-198 merged); card's "already landed" list checked against `main` and still accurate (`open(path)`, `serially`, `teardownDocument`, optional `openDocument`, `renderPath` fallback present) |
| MARXY-43 | no row change; **dispatch next** | ready for two deltas; disjoint from 193 |
| MARXY-48 | no row change; follows 43 | deps Done; shares `apps/desktop/src/commands` with 43; its `main.rs`/`shell/tauri.ts` paths were also held by the MARXY-183 phantom |
| MARXY-44 | no row change | waits on 193 (`app.ts`) and 43 (`commands`) by path; `ready.mjs` sequences it |
| MARXY-194/195/196 | no change | wait on 193 by dependency |
| MARXY-213 (new) | ops, **deps `[MARXY-202]`** (revised in the addendum; first drafted ahead of 202) | the mechanism behind item 1 above |
| MARXY-202 | dispatched anyway and now in review (PR #196); see the addendum | shared paths with 213 |

No split. No drop. MARXY-15, 22 and 115 stay parked or dropped. MARXY-45, 49, 97 and 184 keep their
phase-3 holds, because their deps are not Done.

### MARXY-78, unchanged ruling

It is still `blocked` with 2 attempts. The worktree `../marxy-wt/MARXY-78` (`046362b`) still holds the
WIP. The 2026-09-23 ruling stands and has not been acted on: un-park it for one attempt on that WIP.
One of its two attempts was a headless login failure. It widens its own Paths for
`fixtures/baselines` and `docs/taste-review/queue.md` in its own PR. Split it only if that attempt
fails.

## Ops, this window (roadmap tripwire: ops > half of the last 10 merges)

| Story | Ruling | Why |
| --- | --- | --- |
| MARXY-213 | **Proceeds alongside product**, in the same cycle as MARXY-43, never alone | it stops the fleet redoing merged work on product paths; its paths touch nothing product |
| **MARXY-197** deferral gate | **Proceeds alongside product**, as ruled on 2026-09-23 | the ruling was never executed; the dispatcher died |
| **MARXY-16** ship v0.0.1 | **Deferred** until Phase 3 has two stories In Review | unchanged; `ready.mjs` already excludes it as yielding to 48 |
| **MARXY-202** worktree reserves paths | **Deferred** behind the new story | shared paths; lower-cost failure |
| MARXY-209 (#194, open) | not a planner story; see findings | out-of-plan, already a PR |

Nothing else in the ops lane is `todo`.

## Tripwires checked (`docs/roadmap.md`)

| Tripwire | Result |
| --- | --- |
| Ops > half of the last 10 merges | **Fired (7 of 10).** The cause is dead dispatch (MARXY-208), not ops crowding. The deferrals are above. With 193 running, the next dispatch is one product story (43) and two ops beside it; 48 and 44 follow by path. |
| Weight harness residual > 25 on Linux | Not evaluated. MARXY-22 stays deferred without a Linux desktop (author ruling, 2026-09-22) |
| Cold-start inflation | No recorded number changed this window |
| `justif/core` cannot set ragged text | Not fired; no typeset change merged |
| Reviewer fails the palette task at review #2 | Not evaluable. `docs/taste-review/2026-09-review-2/decisions.md` is still blank, and the palette has no index until MARXY-196 |
| A supported distro on WebKitGTK < 2.50 | Not evaluated |
| Authoring re-enters scope | No |
| A first-time user's first reaction is about a feature | No reviewer session this window |

## Scope pressure

**No cut proposed.** Phase 2 and Phase 3 are not slipping because of how much work they hold. They
have been stalled for two days because nothing was dispatched, and the dispatch machinery is now
repaired. Here is the check for the next pass. If **MARXY-193 and MARXY-43 have not both reached In
Review** by the next delta, with the loop running, the planner proposes the first cut in
`docs/scope.md` order. That cut is the light variant, followed by align table pipes, which is half of
MARXY-43.

## Taste review

No visual work merged this window, so no queue row is owed. MARXY-198 carries its own row. The
MARXY-199 row (`2026-09-reader-typography/`) is still waiting for a person. Keep the palette
re-review (review #2) queued until after MARXY-196.

## Escalation risk

| Story | Risk | Why |
| --- | --- | --- |
| **MARXY-193** | Medium | A residual on top of MARXY-198's `app.ts`; an implementor who does not read the "already landed" list will re-implement `serially`/`teardownDocument`. Criterion 6 (`at` landing through the node map after the grid pass) is timing-sensitive and new ground. It was dispatched once (killed with its shell, no commits); that does not count as a return. |
| **MARXY-48** | Medium–high | The largest Phase 3 story; four features; Rust `reveal_in_editor` and a new crate. The split is the implementor's judgement now the 600-line budget is gone. The most likely to fail twice; if it returns once, the next planner splits it along its four headings. |
| **MARXY-44** | Medium | First story to combine the wide sanitiser policy (MARXY-96) with a persisted per-file store; adjacent to `packages/core` sanitise policy. |
| **MARXY-43** | Low–medium | Align table pipes must keep every byte outside the table and handle wide characters; the fidelity gate is the check, and it is strict. |
| **MARXY-78** | Medium | Already failed twice (one a login failure); one attempt left before a split. |
| **MARXY-197** | Medium | A gate that starts red by design; the allow-list must only shrink. |
| MARXY-213 | Low | Pure functions plus one wiring point; the risk is a fixture test that reaches git. The card forbids it. |

## Findings recorded, no new story

1. **The orchestrator checkout has diverged from `origin/main`.** It is 4 local commits ahead (all
   `orchestration/needs-human.md`: discharging MARXY-198 and PR 188) and 1 behind (MARXY-210). A
   cycle's fast-forward will refuse. Those four commits are board text that belongs in a PR, never
   in the orchestrator's checkout (the MARXY-117 rule). The orchestrator either carries them into
   its next out-of-plan PR, or drops them and resets to `origin/main`. Both bullets only record
   merges that `main` already shows.
2. **PR #194 (MARXY-209) has no board row.** `board-check.mjs` reports `unboarded-pr`. Its own branch
   fixes this with `node orchestration/out-of-plan.mjs row MARXY-209 …`; no other branch may write
   that row.
3. `/private/tmp/marxy-pr147` (`chore/MARXY-166-trigger-ci`) is marked prunable by `git worktree
   list`. The orchestrator may prune it.
4. The worktrees `../marxy-wt/MARXY-16`, `MARXY-43`, `MARXY-197` and `MARXY-202` are left over from
   the killed 2026-09-23 dispatch. MARXY-16's sits at `afd69df` and MARXY-43's at `aaa8c55`, both
   behind `origin/main`. `dispatch.mjs` reuses an existing worktree (`existsSync(wt)`), so a fresh
   attempt must start from `origin/main`, not from the stale base. MARXY-193's worktree was recut at
   `d373abc` by this morning's dispatch and is fine.

## Addendum — 12:40Z re-run

The planner ran again at 12:37Z, while this PR was still open and had turned `DIRTY` against MARXY-209
(#194). It rebased this branch onto `f7f547d` instead of writing a second delta for the same date. The
two conflicts were appends: MARXY-209's row sat next to 212/213 in the CSV and in `deps.json`.

### What changed in four hours

| Item | 08:44Z | 12:40Z |
| --- | --- | --- |
| MARXY-209 | PR #194, no row | **merged**, row on `main`, record `done`. Findings 2 is discharged |
| Orchestrator checkout | 4 ahead, 1 behind | **level with `origin/main`**. Finding 1 is discharged |
| MARXY-183 / MARXY-186 | no record | **still no record.** `ready.mjs` puts both in `blockedByPaths`, so they are held only by MARXY-193's paths and become ready the moment 193 leaves `in_progress` |
| MARXY-193 | dispatched, attempt 1 | **one commit, `52a9717` at 08:44Z, never pushed, no PR, no lease; quiet ~4 h.** The commit stays inside its Paths (plus the CHANGELOG line and a taste-queue row): `app.ts`, `palette/view.ts`, `registry.json`, `test/open-path.test.mjs` |
| MARXY-43 | dispatch next | **in review, PR #199** (attempt 2), `CLEAN`, not reviewed |
| MARXY-197 | alongside product | **in review, PR #198**, `CLEAN`; needs a CODEOWNERS human (`ci.yml`) |
| MARXY-202 | deferred | **dispatched anyway, in review, PR #196**, `CLEAN`; needs a CODEOWNERS human (`cycle.mjs`) |
| MARXY-214 | — | new out-of-plan fix, **PR #197**, row on its branch |
| MARXY-211 | — | out-of-plan research worktree with an uncommitted row; another session owns it; not planned here |
| Loop | stopped | **running** since 09:30Z (`loop.sh status`) |

MARXY-202 was dispatched because its deferral lived only in this unmerged PR. Nothing on `main` told the
board to hold it. That is the cost of a plan PR sitting open: a ruling does not bind until it merges.

### Rulings

1. **`state.mjs done MARXY-183 MARXY-186` is still the first thing to run.** Once MARXY-193 moves to
   review, the next cycle would offer MARXY-183 for a second implementation. MARXY-210 settled itself
   (`adopt.mjs`) and is no longer in the command.
2. **MARXY-193: finish it, do not redispatch it.** The work is committed and inside its Paths. The
   orchestrator runs `pnpm done MARXY-193 --open` in `../marxy-wt/MARXY-193`. If precheck is green, the
   PR opens and joins the review queue. If precheck is red, return it to an implementor as attempt 2
   with the failures, in the same worktree. A fresh cut would throw away `52a9717`.
3. **MARXY-213 now depends on MARXY-202.** Both edit `ready.mjs`, `cycle.mjs` and the README, and 202 is
   already an open PR. Sequencing 213 after it costs one merge. Running both at once would
   put two concurrent diffs into `selectReady`. The card says so.
4. **Review queue first.** Review WIP is 3 of 4 and nothing is ready, so dispatch is idle until
   reviews clear. Spawn the reviewer on **PR #199 (MARXY-43)** first because it is the product lane,
   then #197 (214), then #198 and #196, which also need the author as CODEOWNER.
5. **Scope check (from item 10 below):** MARXY-43 is in review. MARXY-193 is not, but only for want of
   the PR step. No cut yet. The check carries to the next pass unchanged.

### Escalation risk, revised

| Story | Change |
| --- | --- |
| MARXY-193 | Medium → **medium–high**. Its worker went silent after committing, the second silent stop on this story. If `pnpm done` is red, attempt 2 is its last before a split along its criteria (one open path, then the `at`-landing criterion 6) |
| MARXY-43 | Low–medium → **medium**. Attempt 2. A return would be its second, and the next planner would split it into toggle-task-item and align-table-pipes (they are already independent operations) |
| MARXY-213 | Low → **low–medium**. It now builds on MARXY-202's `ready.mjs`; an implementor cut before 202 merges would conflict. `deps.json` prevents it |
| MARXY-202 | new, **medium**: it touches `cycle.mjs`, which holds the lock and the dispatch loop, and it needs a human review |

## Ten-line summary (12:40Z, superseded by the evening addendum)

1. **Run `node orchestration/state.mjs done MARXY-183 MARXY-186` now.** Both merged (#180, #171) with no record; they are held only behind MARXY-193's paths and become ready when 193 leaves `in_progress`.
2. **MARXY-193:** run `pnpm done MARXY-193 --open` in `../marxy-wt/MARXY-193` (commit `52a9717`, in paths, never pushed). Red precheck means attempt 2 in the same worktree, never a fresh cut.
3. **Review queue:** spawn the reviewer on #199 (MARXY-43, product) first, then #197 (MARXY-214). #198 (197) and #196 (202) also need the author as CODEOWNER.
4. Merge this PR (#195) promptly: its rulings bind nothing until they are on `main`, which is why MARXY-202 ran.
5. MARXY-213 depends on MARXY-202 now; dispatch it after #196 merges, alongside a product story.
6. After 193 and 43 land: MARXY-48 (follows 43 on `commands`), MARXY-44 (follows 193 and 43), then 194/195/196.
7. Do not dispatch MARXY-16 this window. MARXY-78: one attempt on its existing WIP when a lane is free.
8. Prune `/private/tmp/marxy-pr147`; leave `../marxy-wt/MARXY-211` alone, since another session owns it.
9. No split, drop, ADR or scope cut. One edge changed on an unmerged row (213 → 202), so there is no post-merge `jira.mjs sync` for existing issues.
10. Next pass: if MARXY-193 and MARXY-43 are not both merged or In Review, propose the `docs/scope.md` cut order (light variant first).

## Addendum — evening re-run

The planner ran again after `origin/main` moved to `eebad4b` (MARXY-193, #200). This branch was DIRTY
against that main; it was merged here instead of opening a second delta for the same date. The
orchestrator checkout was read and not written.

### What changed since 12:40Z

| Item | 12:40Z | Evening |
| --- | --- | --- |
| MARXY-193 | unpushed commit, no PR | **merged** (#200). The `pnpm done` ask is discharged. Its taste-queue row is on `main` |
| MARXY-214 | PR #197 open | **merged** (#197) |
| MARXY-215 | — | **merged** (#201). The cycle now starts one headless reviewer. Investigation 01's "review has no runner" is discharged |
| MARXY-183 / MARXY-186 | no `state.json` record | **done** in the local cache (status lists both). The hand command is discharged. MARXY-213 stays, because the mechanism is still absent |
| MARXY-43 | in review, CLEAN, PR #199 | **returned DIRTY** (attempts unchanged at 2). Conflicts in `commands/`, `render/tasks.ts`, `packages/core/src/operations/` |
| MARXY-197 | in review, PR #198 | **returned DIRTY**. Conflicts in `ci.yml`, `scripts/check-deferrals.mjs`, docs |
| This PR (#195) | open | **returned DIRTY**; this addendum is the rebase |
| MARXY-194 | waiting on 193 | **in review, PR #202, CI red** (`fast`, `ci`). Holds the Phase 2 tail |
| MARXY-202 | in review, PR #196 | unchanged: CODEOWNERS on `cycle.mjs` |
| Ready queue | idle behind review WIP | **empty.** `ready.mjs` offers nothing. 43 and 197 are `in_progress` with open PRs, so reap calls them live and never dispatches a rebase |

### The finding that matters: a DIRTY return has no owner

`processReviewQueue` (`cycle.mjs`) sends a DIRTY pull request back to `in_progress` and names the
files. `ready.mjs` never offers an `in_progress` story. `reap.mjs` treats a row with an open pull
request as live. Three product-or-ops pull requests (43, 197, and this one) sat in that state in one
cycle, and the ready queue went empty. That is why Phase 3 is not moving. It is not a missing feature.

### What this pass does about it

1. **The orchestrator rebases the two product-adjacent PRs by hand this cycle**, in their worktrees,
   merging `origin/main` and pushing: `../marxy-wt/MARXY-43` (PR #199) and `../marxy-wt/MARXY-197`
   (PR #198). A clean merge goes back to review. A conflict that needs a judgement stays `in_progress`
   with the conflict named in `results/KEY.json`. Do not recut either worktree.
2. **One new ops story, MARXY-216**, so the next DIRTY return is rebased once by the cycle
   and parked `blocked` (files named, `needs-human.md` not written) when the rebase still conflicts.
   It depends on MARXY-202, which already edits `cycle.mjs` and is in review. It **proceeds** once 202
   merges, because it is the story that unblocks the phase. It is not dispatched while 43 or 197 are
   still DIRTY and holding `cycle.mjs`'s neighbours.
3. **MARXY-213 now depends on MARXY-216** instead of MARXY-202, so the two `cycle.mjs`
   edits do not run together. MARXY-213 is **deferred**: 183 and 186 are already settled in the cache,
   and the tripwire is still fired (7 of the last 10 merges were ops).

No split. No drop. No ADR. No scope cut: MARXY-193 has merged, and MARXY-43 left review only because
of a conflict, not because the phase is out of work. MARXY-48 stays whole until it returns once.

Investigations under `docs/investigations/` (untracked in the orchestrator checkout, not on this
branch) proposed a dozen further ops stories and several product ones. Under the tripwire, none of
them are filed this pass. The product candidates that are not already stories (contrast gate, reserved
id bypass, copy-section regex, front matter) wait for a window that is not ops-majority. Watching,
remote images, outline and find are already MARXY-194, 44, 97 and 48.

## Tripwires, evening

| Tripwire | Result |
| --- | --- |
| Ops > half of the last 10 merges | **Still fired (7 of 10).** Defer MARXY-213 and MARXY-16. MARXY-216 proceeds after MARXY-202 because it unblocks Phase 3. MARXY-197's rebase is finishing work already in flight, not a new ops dispatch |
| Weight harness, cold-start, `justif/core`, WebKitGTK &lt; 2.50, authoring | Unchanged from the morning table. None fired |
| Palette task at review #2 | Still unevaluable until MARXY-196. `2026-09-review-2/decisions.md` is blank |

## Escalation risk, evening

| Story | Risk | Why |
| --- | --- | --- |
| **MARXY-43** | **High** | Attempts already 2. The DIRTY return did not increment them, but a review return after the rebase would. Split then into toggle-task-item and align-table-pipes; do not split for the conflict |
| **MARXY-48** | Medium–high | Unchanged. Largest Phase 3 story; Rust `reveal_in_editor` is new ground. Split along 48a/48b if it returns once |
| **MARXY-194** | Medium | First watcher in the running app, and PR #202 is red on `fast` and `ci`. A second red after a fix is a return |
| **MARXY-44** | Medium | Unchanged: persisted grants beside the wide sanitiser policy |
| **MARXY-197** | Medium | DIRTY against `ci.yml` and the new gate. The allow-list must only shrink |
| **MARXY-216** | Medium | Edits the review loop. A rebase that pushes a bad merge would land conflict junk. The card parks on conflict and rebases once |
| MARXY-213 | Low–medium | Now behind the dirty-owner story. The hermetic `merged` default is the likely miss |

## Ten-line summary for the orchestrator

1. **Rebase PR #199 (MARXY-43) and PR #198 (MARXY-197) in their existing worktrees** and push. That is why the ready queue is empty. Do not recut them.
2. **MARXY-194 (PR #202) is the product review:** CI is red on `fast` and `ci`. Fix in that worktree; do not start 195 or 196 until it leaves `app.ts`.
3. **MARXY-202 (PR #196)** still needs the author on CODEOWNERS (`cycle.mjs`). It blocks the new dirty-owner story.
4. Merge this PR (#195) after it is green: MARXY-216 depends on 202, and MARXY-213 depends on the dirty-owner story. Dispatch the dirty-owner story once 202 merges and 43 is CLEAN. Do not dispatch MARXY-213 this window.
5. MARXY-183 and MARXY-186 are already Done in the cache. Do not run `state.mjs done` for them again. Do not redispatch MARXY-193; it merged as #200.
6. After 43 is CLEAN and in review: MARXY-48 follows it on `commands`; MARXY-44 follows 43 and the merged 193. Then 195 and 196 after 194.
7. Do not dispatch MARXY-16. MARXY-78: one attempt on its existing WIP when a product lane is free; it widens its own Paths for baselines and the queue row.
8. No split, drop, ADR or scope cut. The next planner splits MARXY-43 only if a reviewer returns it.
9. After this merges, run `node orchestration/jira.mjs sync` — MARXY-213's dependency changed.
10. Leave `../marxy-wt/MARXY-211` alone. Palette re-review stays queued until MARXY-196.
