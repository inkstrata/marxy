---
description: Frozen contracts — these files change only through a story that names them and carries an ADR
globs: ["packages/*/src/contracts/**", "packages/theme/src/tokens.css", "packages/shell-api/src/**"]
alwaysApply: false
---
You are looking at a frozen contract (ADR-0003, ADR-0004, ADR-0008, ADR-0010). Do not edit it unless your story's `Paths` names this file and your PR adds an ADR in `docs/adr/` explaining the change. If your task needs a contract change, stop and report `blocked` with the exact change required; the planner will write the story and the ADR.
